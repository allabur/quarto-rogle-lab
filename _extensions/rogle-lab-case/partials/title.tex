% title-case.tex — portada tipo "artículo" para el formato rogle-lab-case-pdf.
% Uso interno PDI: casos docentes / notas técnicas depositables (RiUNET,
% Zenodo...) — no lo usa el alumno, que sigue con title.tex / rogle-lab-pdf.
%
% Bloque logo (izq.) + metadatos académicos (dcha.) por encima del título,
% y autores con afiliaciones numeradas al estilo de un artículo de congreso
% (authblk), igual que hace citym para las actas.

\usepackage{etoolbox}
\usepackage{authblk}
\renewcommand\Authfont{\bfseries}
\renewcommand\Affilfont{\normalfont\small}
\setlength{\affilsep}{0.5em}

\newcommand{\rogleCaseHeaderBlock}{%
  {\normalfont\mdseries
  \noindent
  \begin{minipage}[t]{0.45\textwidth}
    \IfFileExists{$rogle-lab-resources-dir$logos/rogle-logo.png}{%
      \includegraphics[height=1.4cm]{$rogle-lab-resources-dir$logos/rogle-logo.png}%
    }{}
  \end{minipage}%
  \hfill
  \begin{minipage}[t]{0.5\textwidth}
    \raggedleft\footnotesize
    $if(course.institution)$$course.institution$\\$else$Universitat Politècnica de València\\$endif$
    $if(course.degree)$$course.degree$\\$endif$
    $if(subject)$$subject$\\$else$$if(course.subject)$$course.subject$\\$endif$$endif$
    $if(course.academic-year)$Curso $course.academic-year$$if(course.semester)$ · $course.semester$$endif$\\$endif$
    $if(course.type)$\textbf{$course.type$}$if(course.code)$ -- $course.code$$endif$$endif$
  \end{minipage}
  \par\vspace{1.2em}}%
}

$if(title)$
\title{$title$$if(thanks)$\thanks{$thanks$}$endif$}
\makeatletter
\pretocmd{\@title}{\rogleCaseHeaderBlock}{}{}
\makeatother
$endif$
$if(subtitle)$
\makeatletter
\providecommand{\subtitle}[1]{%
  \apptocmd{\@title}{\par {\large #1 \par}}{}{}
}
\makeatother
\subtitle{$subtitle$}
$endif$

$for(by-author)$
\author[$for(it.affiliations)$$it.number$$sep$,$endfor$]{$it.name.literal$$if(it.orcid)$~\orcidlink{$it.orcid$}$endif$}
$endfor$
$for(by-affiliation)$
\affil[$it.number$]{$it.name$}
$endfor$
\date{$date$}
