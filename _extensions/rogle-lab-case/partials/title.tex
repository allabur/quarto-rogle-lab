% title.tex — partial rogle-lab-case: traduce el YAML del .qmd a las macros
% editoriales de rogle-case-upv.cls. No se escribe a mano en el manuscrito.

\rogleResourcesDir{$rogle-lab-resources-dir$}

$if(course.institution)$\rogleInstitution{$course.institution$}$endif$
$if(course.degree)$\rogleDegree{$course.degree$}$endif$
$if(subject)$\rogleCourseSubject{$subject$}$elseif(course.subject)$\rogleCourseSubject{$course.subject$}$endif$
$if(course.academic-year)$\rogleAcademicYear{$course.academic-year$}$endif$
$if(course.semester)$\rogleSemester{$course.semester$}$endif$
$if(course.type)$\rogleDocType{$course.type$}$endif$
$if(course.code)$\rogleDocCode{$course.code$}$endif$

$if(title)$
\title{$title$$if(thanks)$\thanks{$thanks$}$endif$}
\rogleDocTitle{$title$}
$endif$
$if(subtitle)$
\subtitle{$subtitle$}
$endif$

$for(by-author)$
\author[$for(it.affiliations)$$it.number$$sep$,$endfor$]{$it.name.literal$$if(it.orcid)$~\orcidlink{$it.orcid$}$endif$}
$endfor$
$for(by-affiliation)$
\affil[$it.number$]{$it.name$}
$endfor$
\date{$date$}

$if(license)$
\rogleLicense{$if(license.name)$$license.name$$else$$license$$endif$}{$if(license.url)$$license.url$$endif$}
$endif$
$if(copyright)$
\rogleCopyright{$copyright.year$}{$copyright.holder$}
$endif$

$if(citation)$
\rogleCiteAuthors{$if(authors)$$for(authors)$$it.name.family$, $it.name.given$$sep$; $endfor$$endif$}
\rogleCiteYear{$if(citation.issued.date-parts)$$for(citation.issued.date-parts)$$for(it)$$it$$sep$/$endfor$$endfor$$elseif(citation.issued)$$citation.issued$$else$$date$$endif$}
\rogleCiteTitle{$if(citation.title)$$citation.title$$else$$title$$endif$}
$if(subject)$\rogleCiteSubject{$subject$}$endif$
$if(citation.container-title)$\rogleCiteContainer{$citation.container-title$}$endif$
$if(citation.volume)$\rogleCiteVolume{$citation.volume$}$endif$
$if(citation.issue)$\rogleCiteIssue{$citation.issue$}$endif$
$if(citation.doi)$\rogleCiteDOI{$citation.doi$}$endif$
$if(citation.url)$\rogleCiteURL{$citation.url$}$endif$
$endif$
